#!/usr/bin/node
/// This script is printing 3 lines: (like 1-multi_languages.js) but by using an array of string and a loop
const array = ['C is fun', 'Python is cool', 'JavaScript is amazing'];
for (const element of array) {
  console.log(element);
}
